import 'dart:ui';

abstract class AppOffsets {
  static const Offset zeroOffset = Offset(0, 0);
  static const Offset fourByFourOffset = Offset(4, 4);
  static const Offset eightByEightOffset = Offset(8, 8);
}